export interface Telemetry {tick:number;latitude:number;longitude:number;altitude:number;speed:number;heading:number;battery:number;timestamp:string;progress:number;panels:number;findings:number}
export const simulationConfig={interval:1500,duration:180,buffer:40};
