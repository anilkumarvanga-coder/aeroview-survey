import {User,Project,Role} from './models';
export interface Permissions {all:boolean;clients:boolean;manage:boolean;analytics:boolean;reports:boolean;users:boolean;drones:boolean;live:boolean}
const configurations:Record<Role,Permissions>={super_admin:{all:true,clients:true,manage:true,analytics:true,reports:true,users:true,drones:true,live:true},admin:{all:true,clients:true,manage:true,analytics:true,reports:true,users:false,drones:true,live:true},manager:{all:false,clients:false,manage:false,analytics:true,reports:true,users:false,drones:false,live:true},site_incharge:{all:false,clients:false,manage:false,analytics:false,reports:false,users:false,drones:true,live:true},employee:{all:false,clients:false,manage:false,analytics:false,reports:false,users:false,drones:false,live:false}};
export const permissions=(u:User):Permissions=>u.loginType==='client'?{all:false,clients:false,manage:false,analytics:true,reports:true,users:false,drones:false,live:true}:configurations[u.role!];
export const hasProject=(u:User,p:Project)=>u.loginType==='client'?p.clientId===u.clientId&&u.projectIds.includes(p.id):permissions(u).all||u.projectIds.includes(p.id);
export const roleLabel=(u:User)=>u.loginType==='client'?'Client workspace':u.role!.split('_').map(s=>s[0].toUpperCase()+s.slice(1)).join(' ');
export function canView(u:User,view:string){const p=permissions(u);const restricted:Record<string,boolean>={analytics:p.analytics,comparison:p.analytics,reports:p.reports,clients:p.clients,users:p.users,drones:p.drones,live:p.live};return restricted[view]??['dashboard','projects','overview','map','assets','findings','flights','login'].includes(view)}
