'use client';
import {useState} from 'react';
import {ScanLine,ArrowRight,ShieldCheck,Radio,PanelsTopLeft} from 'lucide-react';
import {Tabs,TabsList,TabsTrigger} from '@/components/ui/tabs';
import {authService} from '../services/authService';
import {roleLabel} from '../permissions';
import {User} from '../models';
export default function Login({onLogin}:{onLogin:(u:User)=>void}){const [mode,setMode]=useState('client');const [email,setEmail]=useState('client@demo.com');const [password,setPassword]=useState('demo123');const [error,setError]=useState('');return <main className="login"><section className="login-story"><div className="brand"><ScanLine size={34}/><span>AeroView<span className="brand-caption">SURVEY & INSPECTION</span></span></div><div><span className="eyebrow">A CLEARER VIEW OF YOUR OPERATIONS</span><h1>Every flight.<br/>Every finding.<br/><em>One workspace.</em></h1><p>From the first survey to the final report, keep your projects and inspection insights in focus.</p><div className="login-facts"><span><PanelsTopLeft/> Project intelligence</span><span><Radio/> Flight monitoring</span><span><ShieldCheck/> Role-based access</span></div></div><footer>DRONE SURVEY PLATFORM <span>01 / SOLAR SURVEY</span></footer></section><section className="login-form"><div className="login-box"><span className="demo">DEMO ENVIRONMENT</span><h2>Welcome to AeroView</h2><p>Sign in to your survey workspace.</p><Tabs value={mode} onValueChange={v=>{setMode(v);setEmail(v==='client'?'client@demo.com':'superadmin@demo.com');setError('')}}><TabsList className="login-tabs"><TabsTrigger value="client">Project / Client</TabsTrigger><TabsTrigger value="firm">Firm team</TabsTrigger></TabsList></Tabs><form onSubmit={e=>{e.preventDefault();try{onLogin(authService.login(email,password,mode))}catch(e){setError((e as Error).message)}}}><label>Email / Project ID<input value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="username"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label>{error&&<p role="alert" className="error">{error}</p>}<button className="primary login-submit">Open workspace <ArrowRight size={18}/></button></form><div className="demo-access"><span>EXPLORE A DEMO ROLE</span><div>{authService.demoUsers().map(u=><button key={u.id} className={email===u.email?'selected':''} onClick={()=>{setMode(u.loginType);setEmail(u.email);setPassword('demo123');setError('')}}>{u.loginType==='client'?'Client':roleLabel(u)}</button>)}</div><p>All demo accounts use <code>demo123</code></p></div><p className="fine">Mock data only. No real drone connection or authentication.</p></div></section></main>}
